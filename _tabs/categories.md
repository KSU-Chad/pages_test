---
# Change the public Modules URL in `_config.yml` under:
# `jekyll-archives.permalinks.category` (for example, `/lessons/:name/`).
# Do not add a `permalink` here; the category-path plugin derives the tab URL.
layout: categories
title: Modules
icon: fas fa-diagram-project
order: 1
---
